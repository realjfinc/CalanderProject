{
  "name": "calander-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for Calander: LLM-based event extraction from uploaded images/PDFs/links (Step 3).",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "node --import tsx --test test/**/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "luxon": "^3.5.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.5",
    "@types/node": "^22.0.0",
    "@types/pdf-parse": "^1.1.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
